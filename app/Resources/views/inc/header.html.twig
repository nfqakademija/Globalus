
<header>
<nav class="navbar navbar-default navbar-fixed-top col-lg-12 col-md-12 col-sm-12">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand latoS" href="{{ path('homepage') }}">
                Testuok<span class="black">!</span>
            </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                {% if is_granted('ROLE_ADMIN') %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin<span class="caret"></span></a>
                    <ul class="dropdown-menu position-topmost">
                        <li><a href="{{ path('adminHome') }}">Pagrindinis</a></li>
                        <li><a href="{{ path('users') }}">Vartotojai</a></li>
                        <li><a href="{{ path('tests') }}">Testai</a></li>

                    </ul>
                </li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user.username is defined %}
                    <li><a class="btn margin-0" href="{{ path('profile') }}">Hi, {{ app.user.username }}</a></li>
                {% else %}
                    <li><a class="btn margin-0 loginButton" href="{{ path('fos_user_security_login') }}">Prisijunk</a></li>
                    <li><a class="btn margin-0 registerButton" href="{{ path('app.registration') }}" >Registruokis</a></li>
                {% endif %}
                <!-- <li><a class="btn margin-0 loginButton">Log in</a></li>
                <li><a class="btn margin-0 registerButton">Sign up</a></li> -->
            </ul>
        </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
</nav>

</header>
