<nav class="amd-menu navbar navbar-default navbar-fixed-top theme_background_color fadeInDown">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">

            <a class="navbar-brand" href="{{ path('homepage') }}">Testuok<span class="black">!</span></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                {% if app.user.username is defined %}
                    <li><a href="{{ path('profile') }}">Hi, {{ app.user.username }}</a></li>
                {% else %}
                    <li><a href="{{ path('fos_user_security_login') }}">Prisijunk</a></li>
                    <li><a href="{{ path('app.registration') }}" class="page-scroll">Registruokis</a></li>
                {% endif %}


            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
